{
  "name": "llmos-presentation",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive talking-head presentation: the video is the prompt, the UI is generated from its transcript timeline.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  }
}
