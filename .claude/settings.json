{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "repo=\"${CLAUDE_PROJECT_DIR:-$(jj root 2>/dev/null || git rev-parse --show-toplevel)}\"; bash \"$repo/.agents/skills/vcs/scripts/session-start.sh\" --hook claude --ide claude",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
